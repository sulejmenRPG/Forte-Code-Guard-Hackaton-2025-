# ⚙️ Railway Environment Variables

## 📋 После успешного deployment добавь эти переменные:

### В Railway Dashboard → Settings → Variables:

```
GITLAB_TOKEN=твой_реальный_токен
GEMINI_API_KEY=твой_реальный_ключ
WEBHOOK_SECRET=my_super_secret_123_qwerty
LLM_PROVIDER=gemini
```

## ✅ Railway автоматически добавит:

```
PORT=8000 (или другой порт)
DATABASE_URL=postgresql://... (после добавления PostgreSQL)
```

---

## 🔧 Полный список переменных:

### Обязательные (добавь вручную):
- `GITLAB_TOKEN` - Personal Access Token из GitLab
- `GEMINI_API_KEY` - API ключ от Google Gemini
- `WEBHOOK_SECRET` - секретный токен для webhook
- `LLM_PROVIDER` - `gemini` или `openai`

### Автоматические (Railway сам создаст):
- `PORT` - порт приложения
- `DATABASE_URL` - подключение к PostgreSQL

### Опциональные:
- `GITLAB_URL` - по умолчанию `https://gitlab.com`
- `MAX_CODE_LENGTH` - по умолчанию `50000`
- `ANALYSIS_TIMEOUT` - по умолчанию `300`
- `MIN_SCORE_FOR_APPROVAL` - по умолчанию `7.0`
- `AUTO_LABEL_MR` - по умолчанию `true`

---

## 🚀 После добавления переменных:

Railway автоматически перезапустит приложение с новыми настройками!

✅ Проверь что `/health` работает
✅ Обнови GitLab webhook URL
✅ Создай тестовый MR
