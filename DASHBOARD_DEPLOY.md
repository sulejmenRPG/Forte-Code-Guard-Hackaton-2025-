# 📊 Развертывание Dashboard

## 🚀 Вариант 1: Streamlit Cloud (РЕКОМЕНДУЕТСЯ)

### Преимущества:
- ✅ **БЕСПЛАТНО**
- ✅ Очень быстро (5 минут)
- ✅ Автоматический деплой при git push
- ✅ HTTPS из коробки

### Шаги:

1. **Зайди на https://streamlit.io/cloud**

2. **Sign in with GitHub**

3. **New app → Deploy a public app**

4. **Настройки:**
   ```
   Repository: твой-username/Forte-Code-Guard-Hackaton-2025
   Branch: main
   Main file path: dashboard_ru.py
   ```

5. **Advanced settings → Environment variables:**
   ```
   API_URL=https://web-production-d3b24.up.railway.app
   ```

6. **Deploy!**

7. **Получишь ссылку:**
   ```
   https://твой-дэшборд.streamlit.app
   ```

---

## 🚂 Вариант 2: Railway (с backend)

### Шаги:

1. **Railway Dashboard → New → Deploy from GitHub**

2. **Выбери репозиторий**

3. **Settings:**
   ```
   Build Command: (оставь пустым)
   Start Command: streamlit run dashboard_ru.py --server.port=$PORT --server.address=0.0.0.0
   ```

4. **Variables:**
   ```
   API_URL=https://web-production-d3b24.up.railway.app
   PORT=${{PORT}}
   ```

5. **Networking → Generate Domain**

---

## 📝 Файлы для Streamlit Cloud

Если Streamlit Cloud требует дополнительные файлы:

### `requirements.txt` (уже есть)
Убедись что включены:
```
streamlit>=1.28.0
pandas>=2.0.0
plotly>=5.17.0
requests>=2.31.0
```

### `.streamlit/config.toml` (создай если нужно)
```toml
[server]
port = 8501
enableCORS = false
enableXsrfProtection = false

[browser]
gatherUsageStats = false
```

---

## ✅ После развертывания:

1. **Открой дэшборд**
   ```
   https://твой-дэшборд.streamlit.app
   ```

2. **Проверь:**
   - ✅ Дэшборд открывается
   - ✅ Статистика отображается
   - ✅ Подключение к backend (если есть данные)

3. **Для презентации:**
   - Скопируй ссылку
   - Добавь в презентацию
   - Можешь открыть в демо!

---

## 🐛 Troubleshooting:

### Дэшборд не подключается к backend?
**Проверь API_URL:**
```python
# В dashboard_ru.py должно быть:
API_URL = os.getenv("API_URL", "http://localhost:8000")
```

**В Streamlit Cloud:**
- Settings → Secrets → Add
- `API_URL=https://твой-backend.up.railway.app`

### Ошибка при деплое?
**Проверь requirements.txt:**
```
streamlit
pandas
plotly
requests
```

---

## 💡 Совет для презентации:

**Открой 2 вкладки:**
1. **Dashboard** - показывай метрики и статистику
2. **GitLab MR** - показывай как AI комментирует

**Сценарий демо:**
```
"Смотрите! (открываю дэшборд)
✓ За неделю проверено 15 MR
✓ Найдено 45 проблем
✓ Сэкономлено 22 часа времени сеньора!

(открываю GitLab MR)
✓ Вот пример - AI нашёл SQL injection
✓ Дал рекомендацию как исправить
✓ Сеньор проверил и подтвердил!"
```

---

**РЕКОМЕНДАЦИЯ:** Используй **Streamlit Cloud** - быстрее всего!
